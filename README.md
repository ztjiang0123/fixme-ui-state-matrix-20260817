# Fix UI state matrix fixture

Disposable repository for exercising IntentLab Code Insights fix states. Each
deliberately long parameter list seeds a separate code-health signal so runs can
be started, stopped, retried, reviewed, and completed without touching an
IntentLab repository.

